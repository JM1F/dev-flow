<UserControl x:Class="dev_flow.Views.SideMenuBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">

    <Grid Background="{DynamicResource BackgroundColor}">
        <Border x:Name="menuContainer" Background="Transparent" HorizontalAlignment="Center" VerticalAlignment="Top">
            <StackPanel x:Name="menuPanel">

                <Button Style="{StaticResource MenuButtonStyle}"
                        Command="{Binding NavigateToPageCommand}"
                        CommandParameter="{Binding HomePageViewModel}">
                    <Image Source="{StaticResource homeDrawingImage}" />
                </Button>

                <Button Background="Transparent" BorderThickness="0"
                        Command="{Binding NavigateToPageCommand}"
                        CommandParameter="{Binding SettingsPageViewModel}">
                    <Image Source="{StaticResource cogDrawingImage}" />
                </Button>
                <Button Content="Theme" Command="{Binding ThemeChangedCommand}" />
            </StackPanel>
        </Border>
    </Grid>

</UserControl>